spring: 
    datasource:
        url: jdbc:h2:./devdb
        username: fatihbaba98
        password: 1234
    jpa:
        hibernate:
            ddl-auto: update 
        properties:
            javax:
                persistence:
                    validation:
                        mode: none

    data: 
      web:
        pageable:
            default-page-size: 5
            max-page-size: 5
    profiles:
        active:
        - dev
---
spring:
    config:
        activate:
            on-profile: production
hoaxify:
    upload-path: storage-production
---
spring:
    config:
        activate:
            on-profile: dev
hoaxify:
    upload-path: storage-dev